/** .TODO.js
 *************************
 * HTML input elements that change on:
 *  keyUp
 *  mouseMoved

 * and use
 *   event.target.value

 * in a class handler function

 *************************
 * async functions making use of async and await

 *************************
 * Generator functions who's iterator yields to a complement
 * generator function who's iterator yields back

 * Try returning an array of iterators that pass .next() to .map()
 * as a cb func during a multi-valued assignment per iteratation

 * Try reduce the same way but for single-valued assignments

 *************************
 * Use tagged template literals in string manipulation

 *************************
 * Use substring method with a negative value to limit an over
 * capped value. Ex:
 */

let CD;
CD = "ABCD".substr(-2);
CD
// CD → 'CD'

/**
 * Use a function's .length prop to change functionality based on
 * args passed
 *
 *************************
 * Use a Array's .every() method to guarentee a set of conditions
 * Use a Array's .filter() method to eliminate a set of values
 * Use a Array's destro syntax to swap indices in a set of values
 * 
 * Use a function's Object param destro syntax
 *
 */
